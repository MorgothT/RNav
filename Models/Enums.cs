namespace Mapper_v1.Models;

public enum ConnectionType
{
    Serial,
    UDP,
    TCP
}
public enum DegreeFormat
{
    Deg,
    Min,
    Sec
}

public enum MapMode
{
    Navigate,
    Measure,
    Target
}