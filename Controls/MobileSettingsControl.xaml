<UserControl x:Class="Mapper_v1.Controls.MobileSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mapper_v1.Controls"
             xmlns:mahapps="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:converters="clr-namespace:Mapper_v1.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Style>
        <Style TargetType="UserControl">
            <Style.Triggers>
                <DataTrigger Binding="{Binding}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>
    <UserControl.Resources>
        <converters:DrawingToMediaColorConverter x:Key="ColorConvert"/>
    </UserControl.Resources>
    <Border BorderBrush="{StaticResource MahApps.Brushes.Accent}"
            BorderThickness="3" CornerRadius="5">
        <StackPanel Orientation="Vertical" Margin="5">
            <TextBlock Text="{Binding Name}" Style="{StaticResource ListTitleStyle}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <StackPanel Orientation="Vertical">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Mobile name:" Margin="0,3" VerticalAlignment="Center"/>
                    <TextBox Text="{Binding Name}" Margin="5,0" Style="{DynamicResource ConnectionInput}" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Primery " VerticalAlignment="Center"/>
                    <CheckBox Content="" IsChecked="{Binding IsPrimery}" VerticalAlignment="Center"/>
                </StackPanel>
                <DockPanel LastChildFill="True" DataContext="{Binding MobileShapeSettings}">
                    <TextBlock Text="Vessel Shape:" Margin="0,3" VerticalAlignment="Center" DockPanel.Dock="Left"/>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <Button Content="..." Click="BrowseButton_Click" />
                        <mahapps:ColorPicker SelectedColor="{Binding ShapeFill, Converter={StaticResource ColorConvert}}" MaxWidth="50" Margin="5,0"                                             />
                        <mahapps:ColorPicker SelectedColor="{Binding ShapeOutline, Converter={StaticResource ColorConvert}}" MaxWidth="50"/>
                    </StackPanel>
                    <TextBox Text="{Binding ShapePath}" Margin="5,0" DockPanel.Dock="Left"/>
                </DockPanel>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
